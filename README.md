# aruodas.lt scrapper

A simple scrapper that finds all the new listings in aruodas.lt and displays them on a simple frontend interface.

Technologies used:
- React for the frontend interface
- Node.js and Express.js for the scrapper server
- Puppeteer to overcome anti-scrapping measures
- Firebase to store the listings
- Heroku to host the server and run it routinely
